"""
sync/__init__.py

Marks the sync directory as a Python package.
Module:
    eims_sync → one-shot, offline-first auto-sync of a saved JSON report
                into the EIMS backend (existing import endpoint).
"""